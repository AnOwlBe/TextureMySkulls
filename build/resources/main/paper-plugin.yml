name: TextureMySkulls
description: <head:base64> now just WORKS
version: '1.0-BETA'

main: owlbe.textureMySkulls.TextureMySkulls
api-version: '26.2'
load: POSTWORLD
folia-supported: true

authors: [ owlbe ]

dependencies:
  server:
    packetevents:
      load: BEFORE
      required: true
